
import AOS from "aos";
AOS.init();

const Home = () => {
  
  // const services = useLoaderData();

    return (
      <div>
        <div>
          
        </div>
        
      </div>
    );
};

export default Home;